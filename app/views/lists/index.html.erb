  <body class="page list index">
      <div class="flash">
      <%if flash[:notice]%>
      <%=flash[:notice]%>
      <%end%>
      </div>
      <div class="list top">
      <h1>投稿一覧</h1>

          <div class="ichiran">

            <% @lists.each do |list| %>
              <div class = "list-index-item">
                  <div class="list-index-item-upper">
                    <%= link_to list.content,"/lists/#{list.id}" %>
                    <%= link_to"HP", list.url,  :target=>["_blank"] %>
                    <%= link_to"レビュー", list.review,  :target=>["_blank"] %>
                  </div>
                  <div class="list-comment">
                  <a>  <%= link_to list.comment,"/lists/#{list.id}" %></a>
                  </div>
           </div>
            <% end %>
           </div>
           <div class="list-index-footer">

           </div>
    </body>
